# 上线前验收清单

> 日期：2026-06-22（阶段 5.6 更新）
>
> 所有项目通过后方可上线。标记 `[ ]` 为待验证，`[x]` 为已通过。
>
> 相关文档：[[stage5.6-deployment-dry-run-plan]]、[[生产部署环境变量模板]]、[[备份与回滚方案]]

## 构建与编译

- [x] 后端编译通过 (`cd backend && mvn clean compile -q`)
- [x] 后端打包通过 (`cd backend && mvn clean package -DskipTests`)
- [x] 前台构建通过 (`cd web && npm run build`)
- [x] 前台类型检查通过 (`cd web && npm run type-check`)
- [x] 后台构建通过 (`cd admin-web && npm run build`)
- [ ] Docker Compose 配置语法正确 (`cd deploy && docker compose config`)
- [ ] Docker Compose 4 容器全部健康 (`cd deploy && docker compose --env-file .env up -d --build && docker compose ps`)
- [ ] 健康检查接口返回 UP (`curl http://localhost/api/health`)

## 认证与安全

- [ ] 注册接口正常 (`POST /api/auth/register`)
- [ ] 登录接口正常 (`POST /api/auth/login`)
- [ ] 登出接口正常 (`POST /api/auth/logout`)，登出后 token 失效
- [ ] 修改密码接口正常 (`POST /api/auth/change-password`)，修改后需重新登录
- [ ] 登录限流生效（同一账号连续 5 次失败后锁定 10 分钟）
- [ ] 默认管理员密码已修改（首次登录后）
- [ ] 敏感信息未提交到 Git（`.env` 不在 Git 跟踪中，配置文件中无硬编码密码）
- [ ] `.gitignore` 包含 `.env`、`node_modules/`、`dist/`、`target/`

## 站点配置

- [ ] 后台站点配置列表正常显示 (`/admin/site`，`GET /api/admin/site-config`)
- [ ] 后台修改配置值成功 (`PUT /api/admin/site-config/{key}`)
- [ ] 前台获取站点配置成功 (`GET /api/site-config`，返回 Map 格式)
- [ ] 前台首页从 API 动态加载站点名称和标语（非硬编码）
- [ ] `recruitOpen` 配置项生效：`true` 时前台显示报名表单，`false` 时隐藏

## 文件上传

- [ ] 后台上传页面可用 (`/admin/upload`)
- [ ] 上传图片成功，返回包含 `url` 的记录 (`POST /api/admin/upload`)
- [ ] 上传文件可通过 Nginx `/uploads/` 路径访问（浏览器直接打开 URL）
- [ ] 文件类型校验生效（仅允许 jpg/jpeg/png/webp）
- [ ] 文件大小限制生效（单文件不超过 10MB）
- [ ] **Magic Bytes 校验生效**：修改扩展名的非图片文件（如将 `.txt` 改为 `.jpg`）被拒绝上传
- [ ] **路径穿越防御生效**：包含 `../` 或 `..\\` 的文件名被拒绝
- [ ] 上传记录列表显示正常 (`GET /api/admin/upload`)
- [ ] 删除上传记录功能正常（同时删除物理文件） (`DELETE /api/admin/upload/{id}`)
- [ ] `deploy/.env` 中 `UPLOAD_BASE_PATH` 已配置正确的目录

## 文章/项目封面上传（阶段 5 新增）

- [ ] 后台文章编辑页封面图片区域显示 CoverUpload 组件（输入框 + 上传按钮）
- [ ] 后台项目编辑页封面图片区域显示 CoverUpload 组件
- [ ] 点击"上传封面"按钮可选择 jpg/jpeg/png/webp 文件
- [ ] 前端校验：非图片类型被拒绝（`before-upload` 校验）
- [ ] 前端校验：超过 10MB 文件被拒绝
- [ ] 上传成功后封面 URL 自动回填到输入框
- [ ] 上传中显示 Loading 状态
- [ ] 上传成功后显示封面预览图（200×120）
- [ ] 手动填写 URL 仍可使用（向后兼容）

## 成员管理

- [ ] 后台成员列表正常显示 (`/admin/members`，`GET /api/admin/members`)
- [ ] 后台创建成员成功 (`POST /api/admin/members`)，含姓名、角色、年级、专业等字段
- [ ] 后台编辑成员成功 (`PUT /api/admin/members/{id}`)
- [ ] 后台删除成员成功，前台不再显示 (`DELETE /api/admin/members/{id}`)
- [ ] 前台成员页从 API 动态加载 (`/members`，`GET /api/members`)，显示已启用成员
- [ ] 前台成员详情可访问 (`GET /api/members/{id}`)
- [ ] 成员头像 URL 可通过文件上传获取后设置

## 方向管理

- [ ] 后台方向列表正常显示 (`/admin/directions`，`GET /api/admin/directions`)
- [ ] 后台创建方向成功 (`POST /api/admin/directions`)
- [ ] 后台编辑方向成功 (`PUT /api/admin/directions/{id}`)
- [ ] 后台启用/禁用方向成功 (`PUT /api/admin/directions/{id}/enable` 和 `/disable`)
- [ ] 禁用方向不在前台显示
- [ ] 后台删除方向成功 (`DELETE /api/admin/directions/{id}`)
- [ ] 前台方向页从 API 动态加载 (`/directions`，`GET /api/directions`)
- [ ] 前台方向详情可访问 (`GET /api/directions/{code}`)

## 文章管理

- [ ] 后台文章列表正常显示，分页可用 (`/admin/articles`)
- [ ] 后台创建文章成功（含标题、Markdown 正文、分类、标签、封面 URL）
- [ ] 后台编辑文章成功
- [ ] 后台发布/下架文章成功 (`PUT /api/admin/articles/{id}/publish` 和 `/offline`)
- [ ] 后台删除文章成功 (`DELETE /api/admin/articles/{id}`)
- [ ] 后台关键词搜索可用
- [ ] 前台文章列表正常显示，分页可用 (`/articles`)
- [ ] 前台文章详情正常渲染（Markdown 渲染，代码高亮）
- [ ] 草稿文章不在前台显示
- [ ] 已下架文章不在前台显示
- [ ] 文章封面图片可正常显示

## 项目管理

- [ ] 后台项目列表正常显示，分页可用 (`/admin/projects`)
- [ ] 后台创建项目成功（含名称、描述、Markdown 正文、封面 URL、GitHub 链接等）
- [ ] 后台编辑项目成功
- [ ] 后台发布/下架项目成功 (`PUT /api/admin/projects/{id}/publish` 和 `/offline`)
- [ ] 后台删除项目成功 (`DELETE /api/admin/projects/{id}`)
- [ ] 后台状态和精选筛选可用
- [ ] 前台项目列表正常显示，分页可用 (`/projects`)
- [ ] 前台项目详情正常渲染
- [ ] 草稿项目不在前台显示
- [ ] 首页精选项目区域正常 (`GET /api/projects/featured`)

## 招新流程

- [ ] 招新开关打开时前台报名页面显示完整表单 (`/recruit`)
- [ ] 招新开关关闭时前台不显示报名表单，显示"招新已关闭"提示
- [ ] 未登录用户无法提交报名（跳转登录页）
- [ ] 登录用户提交报名成功 (`POST /api/applications`)
- [ ] 同一用户只能有一条非撤回报名记录
- [ ] 用户在 PENDING 状态可修改报名 (`PUT /api/applications/my`)
- [ ] 用户在非 PENDING 状态不可修改报名
- [ ] 用户在 PENDING 或 VIEWED 状态可撤回报名
- [ ] 用户可查看自己的报名状态 (`GET /api/applications/my`)
- [ ] 后台报名列表正常显示，分页可用 (`/admin/recruit`)
- [ ] 后台关键词搜索报名记录可用
- [ ] 后台审核：PENDING → VIEWED 流转正常
- [ ] 后台审核：VIEWED → CONTACTED 流转正常
- [ ] 后台审核：CONTACTED → PRELIMINARY_PASSED 流转正常
- [ ] 后台审核：PRELIMINARY_PASSED → INTERVIEWING 流转正常
- [ ] 后台审核：INTERVIEWING → PASSED/REJECTED 流转正常
- [ ] 审核备注可填写并显示
- [ ] 已撤回的记录标记清晰

## 用户管理

- [ ] 后台用户列表正常显示，分页可用 (`/admin/users`)
- [ ] 按用户名/手机号搜索用户可用
- [ ] 按角色/状态筛选用户可用
- [ ] 用户详情可查看 (`GET /api/admin/users/{id}`)
- [ ] 禁用用户成功，禁用后该用户无法登录 (`PATCH /api/admin/users/{id}/status`)
- [ ] 启用已禁用用户成功
- [ ] 修改用户角色成功 (`PATCH /api/admin/users/{id}/role`)
- [ ] 重置用户密码成功 (`POST /api/admin/users/{id}/reset-password`)
- [ ] 管理员不能禁用/修改自己的角色和状态

## 数据概览

- [ ] 后台仪表盘统计卡片显示真实数据 (`/admin/dashboard`)：
  - [ ] 用户总数 (`userCount`)
  - [ ] 文章总数 (`articleCount`)
  - [ ] 已发布文章数 (`publishedArticleCount`)
  - [ ] 项目总数 (`projectCount`)
  - [ ] 已发布项目数 (`publishedProjectCount`)
  - [ ] 待审核报名数 (`recruitPending`)
  - [ ] 报名总数 (`recruitTotal`)
  - [ ] 已通过报名数 (`recruitPassed`)
  - [ ] 已拒绝报名数 (`recruitRejected`)
  - [ ] 成员总数 (`memberCount`)
- [ ] 统计数字与数据库实际数据一致

## 联系我们

- [ ] 联系我们页面显示后台配置的真实联系方式 (`/contact`)
- [ ] 显示内容基于 `GET /api/site-config` 返回的数据
- [ ] 联系方式变更后前台实时更新（刷新页面即可）
- [ ] 未配置时不显示虚假信息

## 分页功能

- [ ] 前台文章列表分页正常（翻页、页码显示、每页条数）
- [ ] 前台项目列表分页正常
- [ ] 后台文章列表分页正常
- [ ] 后台项目列表分页正常
- [ ] 后台报名列表分页正常
- [ ] 后台用户列表分页正常
- [ ] 分页返回字段完整：`records`、`total`、`page`、`pageSize`

## Redis 认证服务（阶段 5 新增）

- [ ] Docker profile 下 Redis 认证服务自动启用（`AUTH_REDIS_ENABLED=true`）
- [ ] Local profile 下内存认证服务自动启用（`redis-enabled=false`，matchIfMissing）
- [ ] Redis 不可达时 Docker 环境后端启动失败（符合预期，Redis 为生产必需）
- [ ] Redis 可用时 Token 黑名单写入 Redis（`codelab:auth:blacklist:{sha256}` key 存在）
- [ ] 登出后 Token 在 Redis 黑名单中，再次使用返回 401
- [ ] 登录失败 5 次后账号锁定 10 分钟（Redis key `codelab:auth:login-lock:{ip}:{username}` 存在）
- [ ] Redis 中 Token 黑名单 key TTL 正确（与 token 剩余有效期对齐）
- [ ] Redis 中登录锁定 key TTL 正确（10 分钟自动过期）
- [ ] Docker Compose 中 Redis 服务去除 `profiles` 限制（始终启动）
- [ ] Docker Compose 中后端 `depends_on` 包含 Redis 健康检查

## 生产环境 — 部署前检查

### 环境变量与密码

- [ ] `deploy/.env` 中 `JWT_SECRET` 已替换为 `openssl rand -base64 64` 生成的强密钥
- [ ] `deploy/.env` 中 `MYSQL_ROOT_PASSWORD` 已替换为至少 16 位强密码
- [ ] `deploy/.env` 中 `MYSQL_PASSWORD` 已替换为至少 16 位强密码
- [ ] `deploy/.env` 中 `REDIS_PASSWORD` 已设置强密码（生产环境不可为空）
- [ ] `deploy/.env` 中所有 `CHANGE_ME_*` 占位符已替换为真实值
- [ ] `.env` 和 `deploy/.env` 均被 `.gitignore` 忽略，未提交到 Git
- [ ] `git check-ignore -v .env deploy/.env` 确认两个文件均被忽略

### 服务器目录结构

- [ ] 服务器已创建 `/opt/codelab/` 目录结构（app/、data/、backup/、logs/、ssl/）
- [ ] `data/uploads/` 目录权限 755，owner 正确
- [ ] `backup/` 目录权限 700
- [ ] `ssl/` 目录权限 600 或 400

### Docker Compose 生产启动

- [ ] `docker compose --env-file deploy/.env -f deploy/docker-compose.yml config` 语法验证通过
- [ ] `docker compose up -d --build` 后 4 个容器全部 healthy（mysql、redis、backend、nginx）
- [ ] `docker compose ps` 显示所有服务状态为 Up (healthy)
- [ ] 健康检查接口返回 `{"status":"UP"}` (`curl -f http://localhost/api/health`)
- [ ] 公开接口全部可访问（directions、articles、projects、members、site-config）
- [ ] 后台登录正常，Token 返回有效
- [ ] 管理接口登录后可访问（dashboard/stats 等）
- [ ] Token 黑名单生效（登出后旧 Token 返回 401 "token已失效"）

### Nginx / HTTPS / 域名

- [ ] Nginx 80 端口对外可访问
- [ ] 前台 SPA 首页正常返回 (`curl http://localhost/` → 200)
- [ ] 后台 SPA 正常返回 (`curl http://localhost/admin/` → 200)
- [ ] Vue Router history fallback 正常（`/admin/dashboard` 返回 index.html）
- [ ] API 代理正常（`/api/*` 转发到 backend:8080）
- [ ] 上传文件通过 Nginx 正常访问（`/uploads/...` 返回正确文件）
- [ ] 域名 DNS A 记录已解析到服务器公网 IP
- [ ] HTTPS 证书已申请（Let's Encrypt 或云服务商），证书文件已放置到 `ssl/` 目录
- [ ] Nginx HTTPS server 块已配置（443 端口监听 + SSL 证书路径）
- [ ] HTTP → HTTPS 301 跳转已配置
- [ ] SSL 证书自动续期已配置（crontab certbot renew）

### 上传目录

- [ ] 上传文件可通过 Nginx `/uploads/` 路径公开访问
- [ ] Nginx 对 uploads 卷使用 `:ro`（只读）挂载
- [ ] `client_max_body_size 10M` 已配置（与后端 10MB 一致）
- [ ] 上传目录权限容器内应用可读写，Nginx 只读

### 安全加固

- [ ] 防火墙已配置，仅开放 80/443 端口
- [ ] 3306（MySQL）、6379（Redis）、8080（Backend）端口未暴露到公网
- [ ] Redis 密码已设置，端口仅内网可访问
- [ ] `.git` 目录未暴露到 Web 可访问路径
- [ ] Nginx 未开启 `autoindex`（目录列表）
- [ ] 上传目录不执行脚本文件（Nginx alias 仅返回静态文件）
- [ ] 默认管理员密码已修改（首次登录后台后立即修改）

### 备份与恢复

- [ ] MySQL 备份脚本已就位（参考 `docs/备份与回滚方案.md` 第 2.5 节脚本模板）
- [ ] MySQL 每日备份 crontab 已配置（推荐每日 03:00）
- [ ] 上传文件备份脚本已就位（参考 `docs/备份与回滚方案.md` 第 3.5 节脚本模板）
- [ ] 上传文件每日备份 crontab 已配置（推荐每日 03:30）
- [ ] MySQL 备份恢复流程已演练（在测试环境验证恢复能正常运行）
- [ ] 上传文件恢复流程已演练
- [ ] Redis AOF 持久化已确认开启（`--appendonly yes`）

### 回滚准备

- [ ] 部署前已记录当前 commit hash（`git rev-parse HEAD`）
- [ ] 部署前已执行 MySQL 全量备份
- [ ] 部署前已执行上传文件备份
- [ ] 回滚步骤已确认（参考 `docs/备份与回滚方案.md` 第五节）

### 日志与监控

- [ ] Nginx 访问/错误日志正常输出
- [ ] 后端应用日志可查看 (`docker compose logs backend`)
- [ ] logrotate 已配置（Nginx 日志按天切割，保留 30 天）
- [ ] （可选）容器监控已配置（ Prometheus / Grafana ）

### 最终确认

- [ ] 所有验收脚本已通过（`scripts/verify-*.sh`）
- [ ] 前台所有页面可正常访问（首页、方向、成员、项目、文章、招新、登录、注册、个人中心、联系我们、404）
- [ ] 后台所有页面可正常访问（仪表盘、方向管理、成员管理、文章管理、项目管理、招新审核、用户管理、上传管理、站点配置）
- [ ] 移动端/响应式布局显示正常
- [ ] 页脚声明存在："本网站为南阳师范学院 Code Lab 实验室自建展示站，非学校官方门户网站"

## 其他页面

- [ ] 首页 `/` — 精选项目、最新文章正常加载
- [ ] 实验室介绍 `/about` — 页面正常显示
- [ ] 登录 `/login` — 登录表单正常
- [ ] 注册 `/register` — 注册表单正常
- [ ] 个人中心 `/profile` — 用户信息显示正常
- [ ] 我的报名 `/my-application` — 报名状态显示正常
- [ ] 404 页面 — 不存在路由显示 404 页面
- [ ] 页脚 — 包含"本网站为南阳师范学院 Code Lab 实验室自建展示站，非学校官方门户网站"声明
- [ ] Logo — 前台页眉、页脚、后台登录页、管理壳层均正确显示

## 验收脚本

```bash
# 招新报名闭环
bash scripts/verify-recruitment-flow.sh

# 文章闭环
bash scripts/verify-article-flow.sh

# 项目闭环
bash scripts/verify-project-flow.sh
```

以上三个脚本全部 PASS 后方可上线。
