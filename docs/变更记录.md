# NYNU Code Lab — 变更记录

## 2026-06-22

### 阶段 5.6：远端同步与部署预演准备

- **分支**：`refine/pc-immersive-frontend`
- **提交**：`4ef9058`
- **内容**：不开发新功能、不做视觉增强、不实际部署。产出部署预演文档：服务器目录结构 `/opt/codelab/`、Docker Compose 生产预演命令、Nginx/HTTPS/域名接入方案（含 Let's Encrypt certbot 自动续期）、服务健康+API+页面验证命令序列。更新上线前验收清单为 40+ 项。未修改核心业务代码。

### 阶段 5.5：本地验证报告

- **分支**：`refine/pc-immersive-frontend`
- **提交**：`98b0889`
- **内容**：全量本地验证。Docker Compose 4 容器 healthy，前后端接口正常。发现并修复 3 个阻塞问题：Redis timeout 格式错误（`3000ms`→`3000`）、缺少 `RedisRepositoriesAutoConfiguration` 排除、上传文件无法通过 Nginx 访问（Nginx 改为 alias 静态文件 + volume 挂载）。收口 .env.example 不一致、变量名不统一问题。

### 阶段 5：生产可用性补强

- **分支**：`refine/pc-immersive-frontend`
- **提交**：`205abc3`、`f333b30`、`3d93303`
- **内容**：
  1. 上传安全升级为 7 层纵深防御（Magic Bytes + 路径穿越防护 + 扩展名白名单 + UUID 重命名 + 日期子目录 + 10MB 限制）
  2. 后台用户管理页面确认已完整接入后端
  3. 文章/项目封面上传接入（新增 `CoverUpload.vue` 组件）
  4. Token 黑名单和登录限流 Redis 化（SHA-256 摘要，`@ConditionalOnProperty` 条件切换，Docker profile 默认启用 Redis）
- **验证**：`backend mvn clean package -DskipTests` ✅ / `web npm run build` ✅ / `admin-web npm run build` ✅ / `git diff --check` ✅

### 阶段 4：站点完善 / 上线级闭环补齐

- **分支**：`refine/pc-immersive-frontend`
- **提交**：`43afd03`
- **内容**：一次性补全全部缺失的后端管理 API 和前端真实数据对接。
  - 后端：新增 5 个模块（site 7 文件 / member 7 文件 / direction 7 文件 / upload 5 文件 / dashboard 2 文件）+ 用户管理补齐（6 文件），合计 34 个 Java 文件，28 个新 API 端点
  - 已有 5 个列表接口全部接入分页（新增通用 `PageResult<T>`）
  - 前台 4 个静态页面改为 API 驱动（方向、成员、联系我们、首页标语）
  - 后台 5 个占位页面替换为完整 CRUD，Dashboard 接入 9 项真实统计
  - 招新状态流转新增 VIEWED/CONTACTED 中间状态（共 8 种）
  - SaTokenConfig 排除路径新增 `/api/members/**`、`/api/directions/**`、`/api/site-config/**`

### Docker / SQL / 部署收口

- **分支**：`refine/pc-immersive-frontend`
- **内容**：`deploy/mysql/init/01-init.sql` 改为唯一权威版本（DROP+CREATE 幂等初始化，含 10 张表完整结构）。删除 4 个历史 SQL 文件。Docker Compose 增加 backend/nginx healthcheck、uploads 数据卷。新增 5 个 .dockerignore 文件、scripts/setup-docker.sh。

### PC 沉浸式前端视觉增强

- **分支**：`refine/pc-immersive-frontend`
- **内容**：
  - 前台新增 `ParticleUniverse`、`CodeRainCanvas`、`EnergyFlowBackground`、`TerminalHero`、`CommandConsole`、`LabControlPanel`、`BuildPipeline`、`GitBranchMap`、`DataCounter` 等组件
  - 首页升级为 PC 大屏沉浸式 Hero + Lab OS + 终端控制台 + 构建流水线
  - 新增 `LabSpatialScene` Three.js WebGL 空间层，`LabSignalField` Canvas 信号网络
  - 后台新增 ECharts 模块状态图，管理壳层增加顶部遥测和模块状态轨
  - 60fps 性能优化（DPR 上限、质量分级、delta time、rAF 合并、visibilitychange 暂停、reduced-motion 降级）
  - 新增依赖：前台 `gsap`、`countup.js`、`@lucide/vue`、`three`，后台 `echarts`
  - 前台 `npm run build` ✅（WebGL chunk > 500 kB 提醒）、后台 `npm run build` ✅（Dashboard chunk > 500 kB 提醒）

### 确认版 Logo 保存与站内接入

- **内容**：确认"终端提示符 + C 形框 + 光标竖线 + 琥珀状态点"方向。保存原始 PNG 到 `docs/assets/logo/`。前台、后台分别接入 SVG mark 与 favicon，增加轻量状态点呼吸和 hover 扫光效果。

### 视觉组件规范沉淀

- **内容**：新增 `docs/前端视觉组件规范.md`，沉淀 PC 沉浸式视觉组件使用场景、动效边界、PC 优先策略、内容真实性约束和后续扩展检查清单。

### 阶段 2：认证安全补齐

- **内容**：新增 `POST /api/auth/logout`（Token 黑名单）、`POST /api/auth/change-password`（BCrypt 校验+强制重新登录）、登录限流（IP+username，5 次失败锁 10 分钟）。前后端三模块构建全部通过。

### 阶段 1：数据库表结构补齐

- **内容**：新增 6 张表（lab_member、lab_direction、lab_site_config、lab_upload_file、lab_article_category、lab_article_tag）。初始化种子数据：5 个技术方向、4 个分类、6 个标签、10 项站点配置（联系方式留空）。

### 第六阶段：上线前全量验收 + 安全加固

- **分支**：`release/mvp-production-readiness`
- **内容**：补齐需求文档、权限异常改为统一业务码 401/403、报名表新增唯一约束、环境变量模板收口、本地 Vite 端口统一、Docker Compose/页面/脚本/权限/SQL/Markdown/Nginx 全量验收通过。

### 第四阶段：项目成果展示

- **内容**：新增 `lab_project` 表 + 后端 project 模块 + 前台项目列表/详情（markdown-it `html:false`）+ 首页精选项目 + 后台项目管理。验证脚本 `verify-project-flow.sh` 21/21 PASS。

### 工程级重命名

- **内容**：`nynu-se-lab` → `nynu-code-lab`。数据库名、Java 包名、Maven artifactId、Docker 资源名、Git remote 全部同步更新。

### 第三阶段：文章管理 + Markdown 学习分享

- **内容**：新增 `lab_article` 表 + 后端 article 模块 + 前台文章列表/详情（markdown-it `html:false`）+ 后台文章管理（CRUD+发布/下架/删除）。验证脚本 `verify-article-flow.sh` 23/23 PASS。

### 容器化验收

- **内容**：全链路验收通过：`docker compose config` ✅ / 4 容器正常启动 ✅ / /api/health 200 ✅。修复 MyBatis-Plus Spring 6.1+ 兼容性问题。

### 招新报名闭环 MVP

- **内容**：新增 `lab_apply_record` 表 + 后端 recruit 模块 + 前台报名/我的报名页面 + 后台报名管理。验证脚本 `verify-recruitment-flow.sh` 18/18 PASS。

### 第一阶段：项目骨架初始化

- **内容**：创建后端(Spring Boot)、前台(Vue3)、后台(Vue3+ElementPlus)三个项目。完成注册/登录/获取当前用户认证闭环。62 个文件。Docker Compose 容器化部署。

---

## 当前状态概览

| 项 | 状态 |
|---|---|
| 分支 | `refine/pc-immersive-frontend` |
| 阶段 | 阶段 5.6 完成，待进入 Stage 6 实际生产部署 |
| 稳定 tag | `stable-mvp-production-ready-20260622` |
| 后端 | 全部管理模块已实现，48MB jar |
| 前台 web | 13 个页面全部接入真实数据，PC 沉浸式视觉完整 |
| 后台 admin-web | 11 个页面全部接入真实数据，Element Plus + ECharts |
| 数据库 | 10 张表，统一在 `deploy/mysql/init/01-init.sql` |
| 容器化 | Docker Compose 4 容器（MySQL/Redis/Backend/Nginx） |
| 接口验证 | 招新 18/18 ✅ / 文章 23/23 ✅ / 项目 21/21 ✅ |

## 已知问题

- `/design-preview` 保留为视觉参考页，使用静态预览数据
- 移动端仅保证不严重崩溃，不做精细适配
- 后台 ECharts Dashboard chunk > 500 kB，构建通过但建议后续拆包
- 后台 npm audit 有 1 moderate + 1 high 既有漏洞
- Nginx 未配置 HTTPS（已提供完整模板和 certbot 方案）
- 备份脚本当前仅提供模板，上线前需落盘并配置 crontab
- MyBatis-Plus 与 Spring 6.1+ 存在类型不兼容（通过 `MybatisPlusSpringFix` 绕过）
- `lab_article_category` 和 `lab_article_tag` 表已建但尚未与文章表关联
- 后端零测试覆盖（`src/test` 目录不存在）
- 浏览量直接在详情接口中递增，无防刷机制

## 下一步建议

1. **生产环境部署** — 填写生产 `deploy/.env`、配置 HTTPS 证书、设置正式域名、配置防火墙、配置 crontab 定时备份
2. **文章分类/标签独立管理** — 将 `lab_article_category` 和 `lab_article_tag` 表与文章表关联
3. **ECharts 体积优化** — Dashboard chunk 拆包或换轻量图表库
4. **移动端适配验证** — 系统性测试核心页面
5. **单元测试补齐** — 后端 `src/test` 目录从零开始建设
6. **项目成员关联** — 项目成员/负责人字段从自由文本改为关联用户或成员表
7. **浏览量防刷** — 增加 IP/UA 去重或时间窗口限制
